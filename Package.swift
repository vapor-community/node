import PackageDescription

let package = Package(
    name: "Node",
    dependencies: [
    ],
    targets: [
        Target(
            name: "Node",
            dependencies: [
            ]
        )
    ]
)
